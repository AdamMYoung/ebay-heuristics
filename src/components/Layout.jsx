import React from 'react';

import { Routes } from './Routes';

export const Layout = () => {
    return (
        <div style={{ background: '#191a1a' }}>
            <Routes />
        </div>
    );
};
