import React from 'react';
import { Button } from 'react-bootstrap';

export const MapControls = () => {
    return (
        <div>
            <Button variant="outline-light">Login to eBay</Button>
        </div>
    );
};
