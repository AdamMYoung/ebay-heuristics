import React from 'react';

import { Routes } from '../routes/Routes';

export const Layout = () => {
    return (
        <div style={{ background: '#191a1a' }}>
            <Routes />
        </div>
    );
};
